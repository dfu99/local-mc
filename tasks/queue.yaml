queue:
- id: q01
  status: completed
  title: Write tests/conftest.py + tests/test_projects.py + tests/test_store.py
  why: Audit ship-list step 1-4. Zero tests today; EchoAgent + httpx already in dev
    deps.
  acceptance: pytest -q passes; >=6 tests; covers Registry CRUD + Store streaming
    append.
  est_minutes: 45
  started: 2026-05-05 01:24
  completed: 2026-05-05 01:30
  result: 23 tests pass (10 projects + 13 store) including 100-delta streaming append
- id: q02
  status: completed
  title: 'tests/test_server.py: REST + sandbox + upload caps'
  why: Audit ship-list step 6. Locks down the security-critical /api/files behavior
    so we can't regress symlink/traversal escapes.
  acceptance: Tests cover happy /api/projects, 400 dup project, 403 traversal, 403
    symlink-out, 413 oversized upload.
  est_minutes: 45
  depends_on:
  - q01
  started: 2026-05-05 01:28
  completed: 2026-05-05 01:35
  result: 21 server tests added; pytest -q tests/ → 44 passed (10 projects + 13 store
    + 21 server)
- id: q03
  status: completed
  title: 'tests/test_chat_ws.py: WebSocket EchoAgent flow'
  why: Audit ship-list step 7. The hottest code path; today only verified by hand.
  acceptance: Asserts user_message → assistant_start → ≥1 delta → done in order, with
    EchoAgent.
  est_minutes: 30
  depends_on:
  - q01
  started: 2026-05-05 01:32
  completed: 2026-05-05 01:38
  result: 7 WS tests added (protocol order, attachments persist, delta/store concat,
    done stats, artifact-diff picks up newly-written file, ping/pong, error on bad
    type); pytest -q tests/ → 51 passed
- id: q04
  status: completed
  title: tests/test_claude_agent.py guarded by shutil.which('claude')
  why: Audit P0 blocker. Even skipped on this machine, it'll execute on Windows and
    tell us what _build_argv breaks.
  acceptance: 'If claude is on PATH: one real turn produces text + done events. If
    not: pytest skips, doesn''t fail.'
  est_minutes: 30
  started: 2026-05-05 01:34
  completed: 2026-05-05 01:42
  commits: 2e74900
  result: 3 real-claude tests passing on Linux against /home/dan/.local/bin/claude
    v2.1.128. Flag combo `-p --output-format stream-json --input-format stream-json
    --verbose` is valid; _parse_event decodes text+session_id+done. Windows verification
    pending — module-level skipif means the test will execute on the Windows target
    once the suite runs there.
  windows_status: still_unverified
- id: q05
  status: completed
  title: .github/workflows/test.yml — Linux + Windows matrix
  why: Audit P1. Catches Windows-specific regressions on every PR; deploy target is
    Windows.
  acceptance: Workflow runs pytest on ubuntu-latest + windows-latest with Python 3.10,
    3.11, 3.12.
  est_minutes: 30
  depends_on:
  - q03
  started: 2026-05-05 01:35
  completed: 2026-05-05 01:46
  result: .github/workflows/test.yml writes a 3x3 matrix (ubuntu-latest, windows-latest,
    macos-latest × py3.10, 3.11, 3.12) plus a build-wheel job that verifies web/ is
    bundled. Real-claude test cleanly skips in CI; cap-tests + sandbox tests run everywhere.
- id: q06
  status: completed
  title: Drop the dead watchfiles dep + thread settings.artifact_globs through server.py
  why: Audit Drop list. Reduces audit surface and stops Settings.artifact_globs from
    being a lie.
  acceptance: watchfiles removed from pyproject.toml/requirements.txt; server.py:329
    passes globs from settings.
  est_minutes: 15
  started: 2026-05-05 01:36
  completed: 2026-05-05 01:50
  result: 'pyproject.toml + requirements.txt: watchfiles removed. server.py: snapshot()
    and diff() now both receive settings.artifact_globs. Pinned-down with a new test
    test_settings_artifact_globs_filter_done_artifacts that sets globs=[''figures/*.png''],
    plants both a .png (matches) and a .csv (does not), and asserts only the .png
    surfaces in the done event.'
- id: q07
  status: completed
  title: Move web/ inside the package as lmc/web/, update create_app web_dir resolution
  why: Audit Drop list. Replaces the `..`-escaping package-data pattern with something
    that's portable across Linux/Windows wheel builds.
  acceptance: python -m build --wheel produces a wheel with lmc/web/{index.html,app.js,style.css};
    server resolves it via importlib.resources or __file__.
  est_minutes: 30
  started: 2026-05-05 01:38
  completed: 2026-05-05 01:55
  result: 'web/{index.html,app.js,style.css} → lmc/web/{...}; server.py uses Path(__file__).parent
    / "web"; pyproject.toml package-data: lmc = ["web/*", "web/**/*"]. Fresh wheel
    bundles lmc/web/{index.html,app.js,style.css} (verified via python -m zipfile
    -l). GET / serves the rendered index.html through TestClient. Suite still 55 passing.'
- id: q08
  status: completed
  title: scripts/install.ps1 — Windows install script
  why: Audit P1. Deploy target is Windows; pipx may not be present on locked-down
    work machines.
  acceptance: PS1 creates venv under %LOCALAPPDATA%\lmc-venv, installs ., drops lmc.cmd
    shim, prints next steps.
  est_minutes: 30
  started: 2026-05-05 01:39
  completed: 2026-05-05 01:58
  result: scripts/install.ps1 (Windows) creates venv at %LOCALAPPDATA%\lmc-venv, runs
    pip install ., drops lmc.cmd in %LOCALAPPDATA%\Microsoft\WindowsApps (on PATH
    by default since Win10 1809), verifies the shim resolves. Plus scripts/install.sh
    twin for POSIX users without pipx (~/.local/lmc-venv + ~/.local/bin/lmc shim).
- id: q09
  status: completed
  title: lmc send <project> "msg" CLI subcommand
  why: Smallest piece of v0.2. Mirrors `mc send` and unblocks scripted workflows on
    the new machine.
  acceptance: lmc send demo 'hello' POSTs to the active session; round-trips through
    the EchoAgent in a test.
  est_minutes: 45
  depends_on:
  - q03
  started: 2026-05-05 01:41
  completed: 2026-05-05 02:02
  result: lmc/cli.py adds `_cmd_send` (asyncio.run on agent.stream, latest-or-new
    session, streams text to stdout, [done] line); --quiet suppresses streaming. tests/test_cli_send.py
    has 6 cases (echo round-trip, --quiet, unknown project → 1, missing path → 1,
    persists to SQLite, subsequent calls reuse latest session). Suite total now 61.
- id: q10
  status: completed
  title: GET /status — minimal status panel HTML
  why: v0.2.0 milestone. Even read-only, it's the visible payoff for switching from
    tmux.
  acceptance: /status returns HTML listing each project + most recent session timestamp;
    lists projects with idle > 1h in red.
  est_minutes: 60
  started: 2026-05-05 01:42
  completed: 2026-05-10 14:35
  result: 'lmc/server.py adds GET /status: HTMLResponse with table of every registered
    project + its latest session (id8, last-active UTC timestamp, humanized idle).
    Stale rows (idle > 1h) get class=stale → red background. _humanize_idle helper
    handles 0s..Nd. tests/test_status.py: 7 cases (HTML basics, no-session marker,
    active session shows idle/sid, 2h backdate flips to stale, multi-project count,
    HTML escape on forged <script> name, parametric humanize). Suite total: 74 passing.'
- id: q11
  status: pending
  title: WebSocket cancel + auto-reconnect
  why: Audit §8 brittleness. Without cancel, a runaway turn means killing the server.
  acceptance: 'Client sends {type: cancel}; server SIGTERMs the subprocess; client
    auto-reconnects with exponential backoff.'
  est_minutes: 60
  notes: 'Earlier completion marker was stale — no cancel/reconnect code in lmc/server.py
    or lmc/web/app.js as of 2026-05-10. Reset to pending.'
- id: q12
  status: pending
  title: Vendor a small markdown renderer (snarkdown or marked)
  why: Audit §8. Today's hand-rolled renderer skips tables/lists/headers; agents emit
    those constantly.
  acceptance: web/markdown.js is a vendored, MIT-licensed file <2KB; renders tables
    and bulleted lists in the chat.
  est_minutes: 30
  notes: 'Earlier in_progress marker was stale — no lmc/web/markdown.js exists as
    of 2026-05-10. Reset to pending.'
- id: q13
  status: completed
  title: Append the audit objective to tasks/objectives.yaml
  why: 'Global CLAUDE.md mandate: every completed objective gets a yaml entry + a
    figure.'
  acceptance: obj-001 entry referencing figures/audit-2026-05-05.png with metrics
    (loc=1582, tests=0, smoke=11/15 green).
  est_minutes: 10
  completed: 2026-05-05 01:22
  result: obj-001 logged with the audit figure
